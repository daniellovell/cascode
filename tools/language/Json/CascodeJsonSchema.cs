namespace Cascode.Language.Json;

/// <summary>
/// JSON schema constants for Cascode JSON serialization.
/// The JSON format version matches the Cascode version (CascodeVersion.Current).
/// </summary>
public static class CascodeJsonSchema
{
    // Version is derived from CascodeVersion - no separate schema version needed.
}
