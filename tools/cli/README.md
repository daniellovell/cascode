# Cascode CLI

Cascode CLI is the command-line interface for the Cascode project, enabling users to link, emit,
render, benchmark, verify, inspect PDK workspaces, and manage simulator/runtime prerequisites from
the terminal.

For general project information, see the [top-level README](../../README.md).
