namespace Cascode.ACIR.Json;

/// <summary>
/// JSON schema constants for ACIR JSON serialization.
/// The JSON format version matches the ACIR version (ACIRVersion.Current).
/// </summary>
public static class AcirJsonSchema
{
    // Version is derived from ACIRVersion - no separate schema version needed.
}
