ACIR 3.0

// Circuit with missing required size reference - EMIT-007 violation
// Device M1 is missing the size pack reference

circuit MissingParamExample
  level EL

  supply VDD
  ground GND

  input IN : analog
  output OUT : analog

  fill:
    nmos M1 (.G--IN, .D--OUT, .S--GND, .B--GND) : nmos
