ACIR 2.0

; Circuit with missing required parameter - EMIT-003 violation
; Device M1 is missing the W (width) parameter

circuit MissingParamExample
  level EL

  supply VDD
  ground GND

  port IN : analog
  port OUT : analog

  fill:
    nmos M1 (G->IN, D->OUT, S->GND, B->GND) : L=180n nmos
