ACIR 2.0

; ML-level circuit with VDD-GND short - ERC-002 violation
; Device M_short has drain on VDD and source on GND

circuit VddGndShortExample
  level ML

  supply VDD
  ground GND

  port IN : analog

  fill:
    nmos M_short (G->IN, D->VDD, S->GND, B->GND) : size=(W=??, L=??, M=??) nmos
