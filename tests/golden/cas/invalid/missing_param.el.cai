VERSION 5.0

primitive NMOS NMOS_Level1(size primSize) {
  device "nmos_level1"
  params {
    W = primSize.W
    L = primSize.L
    m = primSize.M
  }
}


// Circuit with missing required size values - EMIT-007 violation
// Device M1 uses ?? placeholders for required W/L

circuit MissingParamExample {
  level EL

  supply VDD
  ground GND

  input IN : analog
  output OUT : analog

  fill {
    NMOS M1 = new NMOS_Level1(size(W=??, L=??, M=1)) {
      .G--IN
      .D--OUT
      .S--GND
      .B--GND
    }
  }
}
