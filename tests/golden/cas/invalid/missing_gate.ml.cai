VERSION 5.0

primitive PMOS PMOS_Level1(size primSize) {
  device "pmos_level1"
  params {
    W = primSize.W
    L = primSize.L
    m = primSize.M
  }
}


// ML-level circuit with a missing gate binding - ERC-001 violation
// The M_SENSE device has no G-- binding at all

circuit MissingGateExample {
  level ML

  supply VDD
  ground GND

  output OUT : analog

  fill {
    PMOS M_SENSE = new PMOS_Level1(size(W=??, L=??, M=??)) {
      .B--VDD
      .D--OUT
      .S--VDD
    }
  }
}
