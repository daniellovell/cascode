VERSION 5.0

library lib.pdk.sky130.diodes

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive diode_pd2nw_05v5(size primSize) implements Diode {
  device "diode_pd2nw_05v5"
  params {
    AREA = primSize.AREA
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive diode_pd2nw_05v5_hvt(size primSize) implements Diode {
  device "diode_pd2nw_05v5_hvt"
  params {
    AREA = primSize.AREA
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive diode_pd2nw_05v5_lvt(size primSize) implements Diode {
  device "diode_pd2nw_05v5_lvt"
  params {
    AREA = primSize.AREA
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive diode_pd2nw_11v0(size primSize) implements Diode {
  device "diode_pd2nw_11v0"
  params {
    AREA = primSize.AREA
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive diode_pw2nd_05v5_lvt(size primSize) implements Diode {
  device "diode_pw2nd_05v5_lvt"
  params {
    AREA = primSize.AREA
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive diode_pw2nd_05v5_nvt(size primSize) implements Diode {
  device "diode_pw2nd_05v5_nvt"
  params {
    AREA = primSize.AREA
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive diode_pw2nd_11v0(size primSize) implements Diode {
  device "diode_pw2nd_11v0"
  params {
    AREA = primSize.AREA
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive diode_pw2nd_05v5(size primSize) implements Diode {
  device "sky130_fd_pr__diode_pw2nd_05v5"
  params {
    AREA = primSize.AREA
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive extended_drain(size primSize) implements Diode {
  device "sky130_fd_pr__diode_pw2nd_05v5__extended_drain"
  params {
    AREA = primSize.AREA
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive diode_ps2dn(size primSize) implements Diode {
  device "sky130_fd_pr__model__parasitic__diode_ps2dn"
  params {
    AREA = primSize.AREA
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive diode_ps2nw(size primSize) implements Diode {
  device "sky130_fd_pr__model__parasitic__diode_ps2nw"
  params {
    AREA = primSize.AREA
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive diode_pw2dn(size primSize) implements Diode {
  device "sky130_fd_pr__model__parasitic__diode_pw2dn"
  params {
    AREA = primSize.AREA
  }
}

