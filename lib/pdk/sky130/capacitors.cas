VERSION 3.0

library lib.pdk.sky130.capacitors

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive cap_mim_m3_1(size primSize) implements Capacitor {
  device "cap_mim_m3_1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive cap_mim_m3_2(size primSize) implements Capacitor {
  device "cap_mim_m3_2"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcl1d(size primSize) implements Capacitor {
  device "mcl1d"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcl1f(size primSize) implements Capacitor {
  device "mcl1f"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcl1p1(size primSize) implements Capacitor {
  device "mcl1p1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm1d(size primSize) implements Capacitor {
  device "mcm1d"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm1f(size primSize) implements Capacitor {
  device "mcm1f"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm1l1(size primSize) implements Capacitor {
  device "mcm1l1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm1p1(size primSize) implements Capacitor {
  device "mcm1p1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm2d(size primSize) implements Capacitor {
  device "mcm2d"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm2f(size primSize) implements Capacitor {
  device "mcm2f"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm2l1(size primSize) implements Capacitor {
  device "mcm2l1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm2m1(size primSize) implements Capacitor {
  device "mcm2m1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm2p1(size primSize) implements Capacitor {
  device "mcm2p1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm3d(size primSize) implements Capacitor {
  device "mcm3d"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm3f(size primSize) implements Capacitor {
  device "mcm3f"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm3l1(size primSize) implements Capacitor {
  device "mcm3l1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm3m1(size primSize) implements Capacitor {
  device "mcm3m1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm3m2(size primSize) implements Capacitor {
  device "mcm3m2"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm3p1(size primSize) implements Capacitor {
  device "mcm3p1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm4d(size primSize) implements Capacitor {
  device "mcm4d"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm4f(size primSize) implements Capacitor {
  device "mcm4f"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm4l1(size primSize) implements Capacitor {
  device "mcm4l1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm4m1(size primSize) implements Capacitor {
  device "mcm4m1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm4m2(size primSize) implements Capacitor {
  device "mcm4m2"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm4m3(size primSize) implements Capacitor {
  device "mcm4m3"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm4p1(size primSize) implements Capacitor {
  device "mcm4p1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm5d(size primSize) implements Capacitor {
  device "mcm5d"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm5f(size primSize) implements Capacitor {
  device "mcm5f"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm5l1(size primSize) implements Capacitor {
  device "mcm5l1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm5m1(size primSize) implements Capacitor {
  device "mcm5m1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm5m2(size primSize) implements Capacitor {
  device "mcm5m2"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm5m3(size primSize) implements Capacitor {
  device "mcm5m3"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm5m4(size primSize) implements Capacitor {
  device "mcm5m4"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcm5p1(size primSize) implements Capacitor {
  device "mcm5p1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcp1f(size primSize) implements Capacitor {
  device "mcp1f"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcrdld(size primSize) implements Capacitor {
  device "mcrdld"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcrdlf(size primSize) implements Capacitor {
  device "mcrdlf"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcrdll1(size primSize) implements Capacitor {
  device "mcrdll1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcrdlm1(size primSize) implements Capacitor {
  device "mcrdlm1"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcrdlm2(size primSize) implements Capacitor {
  device "mcrdlm2"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcrdlm3(size primSize) implements Capacitor {
  device "mcrdlm3"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcrdlm4(size primSize) implements Capacitor {
  device "mcrdlm4"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcrdlm5(size primSize) implements Capacitor {
  device "mcrdlm5"
  params {
    C = primSize.C
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mcrdlp1(size primSize) implements Capacitor {
  device "mcrdlp1"
  params {
    C = primSize.C
  }
}

