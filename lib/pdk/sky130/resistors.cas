VERSION 5.0

library lib.pdk.sky130.resistors

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mrcl1(size primSize) implements Resistor {
  device "mrcl1"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mrcn(size primSize) implements Resistor {
  device "mrcn"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mrcp(size primSize) implements Resistor {
  device "mrcp"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mrcp1(size primSize) implements Resistor {
  device "mrcp1"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mrcrdlcon(size primSize) implements Resistor {
  device "mrcrdlcon"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mrcvia(size primSize) implements Resistor {
  device "mrcvia"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mrcvia2(size primSize) implements Resistor {
  device "mrcvia2"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mrcvia3(size primSize) implements Resistor {
  device "mrcvia3"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive mrcvia4(size primSize) implements Resistor {
  device "mrcvia4"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive resbody(size primSize) implements Resistor {
  device "resbody"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive reshead(size primSize) implements Resistor {
  device "reshead"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive res_generic_m1(size primSize) implements Resistor {
  device "res_generic_m1"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive res_generic_m2(size primSize) implements Resistor {
  device "res_generic_m2"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive res_generic_m3(size primSize) implements Resistor {
  device "res_generic_m3"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive res_generic_m4(size primSize) implements Resistor {
  device "res_generic_m4"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive res_generic_m5(size primSize) implements Resistor {
  device "res_generic_m5"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive res_generic_nd(size primSize) implements Resistor {
  device "res_generic_nd"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive res_generic_pd(size primSize) implements Resistor {
  device "res_generic_pd"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive res_generic_l1(size primSize) implements Resistor {
  device "sky130_fd_pr__res_generic_l1"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive hv(size primSize) implements Resistor {
  device "sky130_fd_pr__res_generic_nd__hv"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive res_generic_nw(size primSize) implements Resistor {
  device "sky130_fd_pr__res_generic_nw"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive res_generic_po(size primSize) implements Resistor {
  device "sky130_fd_pr__res_generic_po"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive monte(size primSize) implements Resistor {
  device "sky130_fd_pr__res_generic_po__monte"
  params {
    R = primSize.R
  }
}

// geometry: W=[-..-] L=[-..-] M=[-..-]
primitive res_generic_r1(size primSize) implements Resistor {
  device "sky130_fd_pr__res_generic_r1"
  params {
    R = primSize.R
  }
}

