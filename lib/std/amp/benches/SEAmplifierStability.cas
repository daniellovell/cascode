package lib.std.amp.benches;

// Stability bench for single-ended amplifiers (unity-gain non-inverting, loop break at IN_N).
bench SEAmplifierStability {
  spectre_template = "SEAmplifierStability.tpl";
  metrics [
    PhaseMargin: deg
  ]
}

